alpha 				# transparency

border 				# border color

cex 				# char expansion factor

col 				# color for lines and points

fill 				# color for fills

lineheight 			# as multiple of text size

lty 				# line type

lwd 				# line width

pch 				# plotting char

palette 			# func generating color palette

panel, strip		# controls clipping for panels and strips in parameter clip

# parameters for top

top.padding, main, main.key.padding, key.top, key.axis.padding, axis.top, strip, panel, 
axis.panel, between, axis.bottom, axis.xlb.padding, xlab, xlab.key.padding, key.bottom, 
key.sub.padding, sub, bottom.padding

# parameters for layout.heights

left.padding, key.left, key.ylab.padding, ylab, ylab.axis.padding, axis.left, axis.pannel,
strip.left, panel, between, axis.right, axis.key.padding, key.right, right.padding

# parameters for layout.widths

# see ?par in gpar/grid package

