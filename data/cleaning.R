# Data cleaning means identiying problems caused by data collection,
# processing & storage & modifying the data so the issues don't
# interfer with analysis

# FIND DUPLICATES
# index <- duplicated(data)
# data.unique <- data[!duplicated(data), ]
# data.unique <- unique(data)

# SORT
# sort(data)
# sort(data, decreasing=TRUE)

# ORDER
# data[order(data$col), ]